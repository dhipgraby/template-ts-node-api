paths:
  /api:
    get:
      tags:
        - Welcome
      summary: Welcome message
      description: Returns a welcome message.
      responses:
        200:
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
